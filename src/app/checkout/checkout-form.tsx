"use client";
import { FormEvent,useMemo,useState } from "react";
import type { Service } from "../service-data";
import { servicePrice } from "../service-data";
export default function CheckoutForm({services,initial}:{services:Service[];initial?:string}){const [items,setItems]=useState<Record<string,number>>(initial?{[initial]:1}:{}),[choice,setChoice]=useState(services[0]?.slug??""),[ready,setReady]=useState(false);const selected=services.filter(service=>items[service.slug]);const total=useMemo(()=>selected.reduce((sum,service)=>sum+servicePrice(service)*(items[service.slug]??0),0),[items,selected]);const money=(value:number)=>new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(value);const update=(slug:string,amount:number)=>setItems(current=>{const next={...current,[slug]:Math.max(0,(current[slug]??0)+amount)};if(!next[slug])delete next[slug];return next});function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setReady(true)}return <form className="order-builder" onSubmit={submit}><div className="order-catalog"><div className="order-add"><label>Add a service<select value={choice} onChange={event=>setChoice(event.target.value)}>{services.map(service=><option value={service.slug} key={service.slug}>{service.title} — {money(servicePrice(service))}</option>)}</select></label><button type="button" onClick={()=>update(choice,1)}>Add service +</button></div>{selected.length?<div className="order-items">{selected.map(service=><article key={service.slug}><div><span>{service.category}</span><h3>{service.title}</h3><p>{money(servicePrice(service))} each</p></div><div className="order-quantity"><button type="button" onClick={()=>update(service.slug,-1)}>−</button><strong>{items[service.slug]}</strong><button type="button" onClick={()=>update(service.slug,1)}>+</button></div><b>{money(servicePrice(service)*(items[service.slug]??0))}</b></article>)}</div>:<div className="order-empty"><strong>Your package is empty.</strong><p>Select a service above to start building it.</p></div>}</div><aside className="order-summary"><span>YOUR ORDER</span><h2>Project details</h2><label>Full name<input name="name" autoComplete="name" required/></label><label>Email<input name="email" type="email" autoComplete="email" required/></label><label>Phone / WhatsApp<input name="phone" type="tel" autoComplete="tel" required/></label><label>Business name<input name="business" autoComplete="organization"/></label><label>Anything we should know?<textarea name="notes" rows={3}/></label><div className="order-total"><span>Estimated total</span><strong>{money(total)}</strong><small>Final scope and applicable taxes are confirmed before payment.</small></div><button className="button button-primary" disabled={!selected.length}>Proceed to payment →</button>{ready&&<p className="order-ready" role="status"><strong>Your order is ready.</strong> The checkout data and pricing flow are complete. Secure gateway processing can now be connected without redesigning this page.</p>}</aside></form>}
