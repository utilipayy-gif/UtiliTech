import Link from "next/link";
import type { Service } from "./service-data";
export function ServicePackages({service}:{service:Service}){if(!service.packages?.length)return null;return <section className="nsx-section service-packages"><div className="package-heading"><span>02 / CHOOSE YOUR PACKAGE</span><h2>Pick the level that fits your <em>need.</em></h2><p>Each package is designed for a different stage and requirement. Compare what&apos;s included before adding one to your order.</p></div><div className="package-grid">{service.packages.map((item,index)=><article key={item.id} className={index===1?"package-featured":""}>{index===1&&<b className="package-badge">MOST POPULAR</b>}<span>{String(index+1).padStart(2,"0")} / {item.name}</span><h3>{item.heading}</h3><p>{item.description}</p><strong>{new Intl.NumberFormat("en-IN",{style:"currency",currency:"INR",maximumFractionDigits:0}).format(item.price)}</strong><ul>{item.features.map(feature=><li key={feature}>{feature}</li>)}</ul><Link className="button button-primary" href={`/checkout?service=${service.slug}&package=${item.id}`}>Choose {item.name} →</Link></article>)}</div></section>}
